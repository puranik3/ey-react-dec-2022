type Props = {
    id: string | number
};

const SessionsList = ( { id } : Props ) => {
    return (
        <div>
            sessions list works!
        </div>
    );
}
 
export default SessionsList;